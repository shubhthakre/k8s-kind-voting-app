apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: db # Corrected indentation here
  namespace: voting-app
spec:
  type: ClusterIP
  ports:
  - name: "db-service"
    port: 5432
    targetPort: 5432
  selector:
    app: db
